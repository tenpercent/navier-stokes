How to build:

1. $> cd build
2. $> cmake ..
3. $> make

How to run:

1. $> ./build/bin/PDESolver

Results of current run are stored in ./build/bin/results.csv. Might want to collect them in separate directory, some time.

